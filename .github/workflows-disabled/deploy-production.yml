name: Deploy to Production

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

env:
  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GCP_REGION: asia-south1
  SERVICE_NAME: nobrainy-production
  IMAGE_NAME: asia-south1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/nobrainy/app

jobs:
  ci:
    uses: ./.github/workflows/ci.yml

  deploy:
    name: Build & Deploy to Production
    runs-on: ubuntu-latest
    needs: [ci]
    permissions:
      contents: read
      id-token: write
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.GCP_REGION }}-docker.pkg.dev --quiet

      - name: Build Docker image
        run: |
          docker build \
            -t ${{ env.IMAGE_NAME }}:${{ github.sha }} \
            -t ${{ env.IMAGE_NAME }}:production-latest \
            -t ${{ env.IMAGE_NAME }}:latest \
            .

      - name: Push Docker image
        run: |
          docker push ${{ env.IMAGE_NAME }}:${{ github.sha }}
          docker push ${{ env.IMAGE_NAME }}:production-latest
          docker push ${{ env.IMAGE_NAME }}:latest

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE_NAME }}
          region: ${{ env.GCP_REGION }}
          image: ${{ env.IMAGE_NAME }}:${{ github.sha }}
          env_vars: |
            NODE_ENV=production
          secrets: |
            DATABASE_URL=nobrainy-database-url-prod:latest
            NEXTAUTH_SECRET=nobrainy-nextauth-secret-prod:latest
            NEXTAUTH_URL=nobrainy-nextauth-url-prod:latest
            GOOGLE_CLIENT_ID=nobrainy-google-client-id:latest
            GOOGLE_CLIENT_SECRET=nobrainy-google-client-secret:latest
          flags: |
            --port=3000
            --memory=1Gi
            --cpu=2
            --min-instances=0
            --max-instances=10
            --allow-unauthenticated
            --cpu-boost
            --add-cloudsql-instances=${{ secrets.GCP_PROJECT_ID }}:${{ env.GCP_REGION }}:nobrainy-db

      - name: Run database migrations
        run: |
          gcloud run jobs create nobrainy-migrate-prod \
            --image=${{ env.IMAGE_NAME }}:${{ github.sha }} \
            --region=${{ env.GCP_REGION }} \
            --set-secrets=DATABASE_URL=nobrainy-database-url-prod:latest \
            --add-cloudsql-instances=${{ secrets.GCP_PROJECT_ID }}:${{ env.GCP_REGION }}:nobrainy-db \
            --command="npx" \
            --args="prisma,migrate,deploy" \
            --max-retries=1 \
            --task-timeout=300s \
            2>/dev/null || \
          gcloud run jobs update nobrainy-migrate-prod \
            --image=${{ env.IMAGE_NAME }}:${{ github.sha }} \
            --region=${{ env.GCP_REGION }} \
            --set-secrets=DATABASE_URL=nobrainy-database-url-prod:latest \
            --add-cloudsql-instances=${{ secrets.GCP_PROJECT_ID }}:${{ env.GCP_REGION }}:nobrainy-db
          gcloud run jobs execute nobrainy-migrate-prod \
            --region=${{ env.GCP_REGION }} \
            --wait

      - name: Show deployment URL
        run: |
          URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region=${{ env.GCP_REGION }} \
            --format="value(status.url)")
          echo "Production deployed to: $URL"
          echo "## Deployed to Production" >> $GITHUB_STEP_SUMMARY
          echo "URL: $URL" >> $GITHUB_STEP_SUMMARY
